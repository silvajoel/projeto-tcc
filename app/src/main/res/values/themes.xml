<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.TCC" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/blue_teal</item>
        <item name="colorPrimaryVariant">@color/blue_teal</item>
        <item name="colorOnPrimary">@color/blue_teal</item>
        <item name="colorSecondary">@color/blue_teal</item>
        <item name="colorSecondaryVariant">@color/blue_teal</item>
        <item name="colorOnSecondary">@color/blue_teal</item>
        <item name="android:statusBarColor">@color/blue_teal</item>
    </style>

    <!-- Base application theme. -->
    <style name="Theme.Fab_Bottom_app_bar" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="colorPrimary">#0F9D58</item>
        <item name="colorPrimaryVariant">#0F9D58</item>
        <item name="colorOnPrimary">#000</item>
    </style>

    <style name="DialogAnimation">
        <item name="android:windowEnterAnimation">@anim/slide_in</item>
        <item name="android:windowExitAnimation">@anim/slide_out</item>
    </style>

    <style name="Theme.TCC" parent="Base.Theme.TCC" />

    <style name="Theme.TCC.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="Theme.TCC.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="Theme.TCC.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
    
    <style name="ButtomSheetDialog" parent="Theme.Design.BottomSheetDialog">
        <item name="bottomSheetStyle">
            @style/BottomSheetStyle
        </item>
    </style>
    
    <style name="BottomSheetStyle" parent="Widget.Design.BottomSheet.Modal">
        <item name="android:background">@android:color/transparent</item>
    </style>
</resources>